# Starmong 앱 단점 분석

## 1. 구조적 문제

| 문제 | 설명 |
|------|------|
| **단일 파일 모놀리스** | `starmong_gemini.jsx`가 ~500줄 단일 파일. Menu/Map/Game 뷰가 전부 하나의 컴포넌트 안에 존재 |
| **라우팅 부재** | `useState('menu')`로 뷰 전환 → 브라우저 뒤로가기 불가, URL 공유 불가 |
| **SoundEngine 전역 IIFE** | 모듈 로드 시점에 즉시 실행되어 테스트/SSR 환경에서 `window` 참조 오류 가능 |
| **사용하지 않는 레거시 파일** | `src/starmong.jsx` (Canvas 프로토타입)가 코드에 남아 있음 |

## 2. 게임플레이 UX

| 문제 | 설명 |
|------|------|
| **선 취소(undo) 불가** | 잘못 연결한 선을 되돌릴 수 없어 실수하면 재시작뿐 |
| **터치/드래그 미지원** | 클릭 기반만 지원 → 모바일에서 별을 드래그하여 연결하는 직관적 인터랙션 없음 |
| **힌트 페널티 없음** | 힌트를 무제한 사용해도 점수 차감이 없어 의미가 약함 |
| **점수 체계 단조로움** | 연결당 고정 150점 + 시간 보너스뿐. 연속 정답/무실수 보너스 등 없음 |
| **완료 후 재도전 의미 부족** | 이미 완료한 별자리를 다시 해도 점수만 쌓임, 최고 기록/랭킹 없음 |

## 3. 에러 처리 & 안정성

| 문제 | 설명 |
|------|------|
| **localStorage 쓰기 실패 미처리** | 시크릿 모드/용량 초과 시 `setItem` 오류 가능 |
| **Web Audio 초기화 미보호** | `AudioContext` 생성 실패 시 try-catch 없어 앱 크래시 가능 |
| **parseInt 파싱 불완전** | `parseInt(localStorage.getItem(...))` — `NaN` 반환 가능성 (`|| 0`으로 커버되긴 하지만 `Number()`가 더 안전) |

## 4. 성능

| 문제 | 설명 |
|------|------|
| **Miniature 컴포넌트 최적화 부재** | 도감에서 완료된 별자리마다 SVG를 매 렌더마다 재생성. `React.memo` 미적용 |
| **별 검색 O(n)** | 선/경로 렌더링 시 `stars.find(s => s.id === p[0])`를 반복 호출 → ID→별 맵 캐싱 없음 |
| **tierColors 객체 매 렌더 생성** | 렌더 함수 내부에서 매번 `tierColors` 객체 리터럴을 새로 만듦 |

## 5. 접근성 & 반응형

| 문제 | 설명 |
|------|------|
| **키보드 접근성 부재** | 별 버튼에 aria-label 없음, 탭 순서 불명확 |
| **데스크톱 레이아웃 미최적화** | `max-w-md` 고정으로 큰 화면에서 좌우 여백만 큼 |
| **다국어 미지원** | 모든 텍스트가 하드코딩된 한국어 |

## 6. 데이터 & 진행도

| 문제 | 설명 |
|------|------|
| **진행도 초기화 기능 없음** | 데이터 리셋 버튼이 없어 localStorage 수동 삭제 필요 |
| **기기 간 동기화 불가** | localStorage 기반이라 다른 기기에서 이어할 수 없음 |
| **별자리 설명 빈약** | `desc` 필드가 "안드로메다자리(Andromeda)" 같은 이름 반복 수준 → 교육적 가치 부족 |

## 우선순위 제안

가장 임팩트가 큰 순서:

1. **컴포넌트 분리** (Menu/Map/Game) — 유지보수성
2. **선 취소(undo) 기능** — UX 개선
3. **SoundEngine 에러 처리** — 안정성
4. **힌트 페널티 + 점수 다양화** — 게임성
5. **접근성 개선** (aria-label, 키보드 내비게이션)
