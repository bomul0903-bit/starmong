# Starmong 앱 단점 분석 (2025-02-08 업데이트)

> 최근 개선: 게임 로직 분리(`gameLogic.js` + 28개 테스트), 9단계 별 수 기반 난이도 체계, 사운드 ADSR/레이어링 적용

## 1. 구조적 문제

| 문제 | 설명 | 심각도 |
|------|------|--------|
| **단일 파일 모놀리스** | `starmong_gemini.jsx`가 ~520줄. Menu/Map/Game 뷰 + 모달 2개가 하나의 컴포넌트 | 높음 |
| **라우팅 부재** | `useState('menu')`로 뷰 전환 → 브라우저 뒤로가기 불가, URL 공유 불가 | 중간 |
| **SoundEngine 전역 IIFE** | 모듈 로드 시점에 즉시 실행 → 테스트/SSR에서 `window` 참조 오류 가능 | 중간 |
| **레거시 파일 잔존** | `src/starmong.jsx` (Canvas 프로토타입)가 사용되지 않으면서 남아 있음 | 낮음 |

## 2. 게임플레이 UX

| 문제 | 설명 | 심각도 |
|------|------|--------|
| **선 취소(undo) 불가** | 잘못 연결한 선을 되돌릴 수 없음. 실수 시 재시작뿐 | 높음 |
| **터치/드래그 미지원** | 클릭 기반만 지원. 모바일에서 별 사이를 드래그하여 연결하는 직관적 인터랙션 없음 | 높음 |
| **힌트 페널티 없음** | 힌트 무제한 사용 가능, 점수 차감 없어 의미가 약함 | 중간 |
| **점수 체계 단조로움** | 연결당 고정 150점 + 시간 보너스뿐. 연속 정답/무실수 콤보 보너스 없음 | 중간 |
| **완료 후 재도전 의미 부족** | 이미 완료한 별자리를 재도전해도 점수만 누적. 최고 기록/별점 등급 없음 | 낮음 |

## 3. 에러 처리 & 안정성

| 문제 | 설명 | 심각도 |
|------|------|--------|
| **localStorage 쓰기 실패 미처리** | `setItem` 호출에 try-catch 없음. 시크릿 모드/용량 초과 시 오류 가능 | 중간 |
| **Web Audio 초기화 미보호** | `AudioContext` 생성/`playTone` 호출에 try-catch 없음 → 미지원 환경에서 앱 크래시 가능 | 중간 |

## 4. 성능

| 문제 | 설명 | 심각도 |
|------|------|--------|
| **Miniature 컴포넌트 최적화 부재** | 도감에서 완료된 별자리마다 SVG를 매 렌더마다 재생성. `React.memo` 미적용 | 낮음 |
| **별 검색 O(n) 반복** | `stars.find(s => s.id === p[0])`를 선/경로 렌더링마다 호출. ID→별 맵 캐싱 없음 | 낮음 |
| **tierColors 객체 매 렌더 생성** | 렌더 함수 내부에서 9색 tierColors 객체 리터럴을 매번 새로 생성 | 낮음 |

## 5. 접근성 & 반응형

| 문제 | 설명 | 심각도 |
|------|------|--------|
| **키보드 접근성 부재** | 별 버튼에 `aria-label` 없음, 탭 순서 불명확, 스크린리더 미고려 | 중간 |
| **데스크톱 레이아웃 미최적화** | `max-w-md` 고정 → 태블릿/데스크톱에서 좌우 여백만 큼 | 낮음 |

## 6. 데이터 & 진행도

| 문제 | 설명 | 심각도 |
|------|------|--------|
| **진행도 초기화 기능 없음** | 리셋 버튼이 없어 localStorage 수동 삭제 필요 | 중간 |
| **별자리 설명 빈약** | `desc`가 "안드로메다자리(Andromeda)" 수준의 이름 반복 → 교육적 가치 부족 | 중간 |
| **기기 간 동기화 불가** | localStorage 기반이라 다른 기기에서 이어하기 불가 | 낮음 |

## 우선순위 제안

| 순위 | 작업 | 카테고리 | 기대 효과 |
|------|------|----------|-----------|
| 1 | **컴포넌트 분리** (Menu/Map/Game 뷰 + SoundEngine) | 구조 | 유지보수성 대폭 향상, 테스트 용이 |
| 2 | **선 취소(undo) 기능** | UX | 게임 만족도 직접 개선 |
| 3 | **SoundEngine + localStorage 에러 처리** | 안정성 | 크래시 방지 |
| 4 | **힌트 페널티 + 콤보 점수** | 게임성 | 리플레이 동기 부여 |
| 5 | **접근성 개선** (aria-label, 키보드) | 접근성 | 사용자 범위 확대 |
| 6 | **별자리 교육 콘텐츠 보강** | 콘텐츠 | 앱의 교육적 가치 |
